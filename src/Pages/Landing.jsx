import React from 'react';
import styled from '@emotion/styled'

const Landing = ({}) => (<Header>blah</Header>)

const Header = styled.div(({theme}) => ({
    color: theme.color,
}))

export default Landing;
